# The reference contract pack: vital-sign extraction from clinical notes.
#
# This is a worked example as much as it is a corpus. It is the pack `spec/` describes and
# the one the profile in src/profiles/clinical/ reads, so a project writing its first pack
# has something complete to copy: a prompt with no placeholders, a schema whose property
# order is pinned to a golden, an answer key that states what each case discriminates, and
# a model declaration naming the weights the numbers were measured on.
#
# THE NOTES ARE SYNTHETIC. They were written for this corpus and are not derived from,
# edited from, or inspired by any real record. Anything added here must meet the same
# standard: this directory is the single most likely place for patient data to enter a
# public repository, and there the mistake cannot be taken back.
#
# Paths are relative to this file. The harness reads files by KEY, never by path, so
# moving or renaming a contract is a change here rather than a change in code.

# The format version, and it is a real one: it was bumped to 2 the day two keys became
# load-bearing. `[clinical.quoteVerification].accentSensitive` is REQUIRED and a pack without
# it does not load; `[clinical].corpusSynthetic` decides whether a trace may hold completions
# verbatim, and a pack that omits it is treated as holding real records. Both were added
# while `spec` said 1, which meant every pack written the day before stopped loading and got
# a message about a missing TOML key rather than a statement that the format had moved. See
# SPEC_CHANGES in src/core/pack.ts, which is what a loader quotes when it finds one absent.
spec = 3
name = "clinical"

# Per-case source documents. `{case}` is substituted with the eval case name, which is why
# a case's `name` and its document's filename are the same string.
#
# A TABLE rather than one template, because this pack now holds two corpora that are not the
# same kind of thing. `default` is the written notes, read by three tasks. `transcript` is the
# dictated consultations, read by the fourth — and filing those under `notes/*.note.txt` would
# mislabel them in the one directory of this repository where what a document IS carries the
# most weight. A pack with one corpus still writes `documents = "..."`; that is the `default`
# kind and nothing about it changed.
[documents]
default    = "notes/{case}.note.txt"
transcript = "transcripts/{case}.transcript.txt"

[files]
# The system prompt. NO placeholders: the note is the `user` message, not spliced into this
# file. That is what keeps these bytes identical across every case, which is what lets
# llama-server's `cache_prompt` reuse the KV cache from one note to the next — and what
# would make a byte comparison against a second runtime mean something.
vitalSignsPrompt       = "prompts/vital-signs.md"
vitalSignsSchema       = "schemas/vital-signs.schema.json"
# The schema AS SERIALIZED. Property order is compiled into the GBNF grammar, so the bytes
# are pinned rather than the structure — a reordering that deep-equality would call
# identical changes what the model is forced to emit first. See the schema's x-notes.
vitalSignsSchemaGolden = "schemas/vital-signs.golden.json"
vitalSignsCases        = "evals/vital-signs-cases.json"

# --- Patient summary: history / usual medication / pending, over a whole record. --------
# Set extraction rather than transcription, and the task whose INPUT is assembled from many
# notes — see [clinical.summaryAssembly] below, which is part of the contract for the same
# reason the prompt is: two runtimes that assemble differently grade different inputs while
# appearing to share a prompt.
summaryPrompt       = "prompts/patient-summary.md"
summarySchema       = "schemas/patient-summary.schema.json"
summarySchemaGolden = "schemas/patient-summary.golden.json"
summaryCases        = "evals/patient-summary-cases.json"

# --- Note formatting: one note rewritten into the four sections a clinician reads. ------
# The same single-note input as vital signs, read differently: that pass pulls numbers out,
# this one reorganises the prose. Its cases name a note in the vital-signs corpus rather
# than carrying their own, so 21 notes serve three tasks.
noteFormatPrompt       = "prompts/note-format.md"
noteFormatSchema       = "schemas/note-format.schema.json"
noteFormatSchemaGolden = "schemas/note-format.golden.json"
noteFormatCases        = "evals/note-format-cases.json"

# --- Dictated transcripts: the SAME four sections, from speech instead of prose. --------
# No schema of its own, deliberately. A clinician reads one structure, so the transcript task
# emits the note-format contract byte for byte — the keys above are reused rather than copied,
# and an edit to that schema moves both tasks or neither. What this task has of its own is a
# PROMPT (speech has retractions, dictated punctuation and asides that prose does not) and a
# CORPUS (transcripts/, reached through the `transcript` documents kind above).
transcriptPrompt = "prompts/transcript.md"
# The SAME contract, written in Spanish, chosen by the language of the transcript rather than
# by a setting. A prompt is instructions in some language, and a small model under pressure
# answers in the language it was instructed in: the rule that says "the transcript decides"
# holds while every item is a quote with words deleted, and stops holding the moment the model
# INVENTS an item, because invented text has no quote to stay in the language of. That is not
# hypothetical here — see `tr-es-13-control`, where the observed failure is a plan item nobody
# dictated, arriving in English out of a Spanish consultation.
#
# A per-language prompt is therefore not a translation of a document; it is the fallback path
# made to fail in the right language. Absent this key, a reader falls back to `transcriptPrompt`
# and behaves exactly as it did before — which is what the two runtimes do for every language
# that has no file here.
transcriptPromptEs = "prompts/transcript.es.md"
transcriptCases  = "evals/transcript-cases.json"

# --- The same contract again, for a transcript with TWO SPEAKERS in it. ----------------
# A consultation transcribed as the room heard it is not a dictation, and the difference is not
# a matter of degree: the record's facts are no longer all in the record-keeper's voice. The
# presenting complaint is quotable only from the patient, a clinician who names a condition to
# ASK about it has asserted nothing, and a dose the clinician states can be corrected by the
# patient in the next turn — a retraction whose only marker is who is talking, which is the one
# thing the dictation prompt's "the speaker's last word is the only word" cannot express.
#
# IT IS A SECOND PROMPT RATHER THAN A SECTION IN THE FIRST, and that was measured rather than
# assumed. The rules below lived in prompts/transcript.md for exactly one run: derivation
# recovered and fabrication did not, and two DICTATIONS that had nothing to do with dialogue lost
# items to a paragraph about turn labels (tr-en-02 83→67%, tr-en-08 86→71%). A prompt is not a
# patch — a rule written for one input shape is read against every input — so the shapes get a
# file each and the seventeen dictations stop paying for speech they do not contain. See
# RESULTS.md, 'a dialogue-aware prompt'.
#
# Absent these keys, every transcript takes `transcriptPrompt` and this pack behaves exactly as
# it did before they existed.
dialoguePrompt   = "prompts/dialogue.md"
dialoguePromptEs = "prompts/dialogue.es.md"

# --- The medication pass: the same transcript again, asked for one section. -------------
#
# A SECOND CALL PER DICTATION, and unlike the repair it is not a correction — it is the section
# being read for the first time by a model that has nothing else to decode. That is the whole
# mechanism, and it was measured before it was written: over nine dictations in both languages,
# the four-section reading returned 23/25 drugs and 22/25 doses, while the same transcripts asked
# for medication alone returned 25/25 and 25/25. The three recoveries are not cosmetic — an
# amlodipine whose dose was corrected mid-dictation, a donepezilo, and an amoxicillin whose dose
# came back as "to complete the course" — and a drug missing from a medication list is a
# prescription absent from the record.
#
# DICTATIONS ONLY, and that is [clinical.medicationPass].shapes below rather than a fact about
# the code. On the three two-speaker consultations the same pass makes the reading WORSE: it
# returned six items for three drugs on tr-en-19, emitting the superseded cross-speaker dose
# beside the corrected one, and derivation over the dialogues fell to 5/12 because a fact split
# across two turns needs the unbroken run quoted and the pass keeps stitching fragments. The
# dialogue prompt exists because a consultation is a different input; this pass does not have it.
#
# IT RUNS BY DEFAULT on the shapes below, and it was opt-in for exactly one measured run. That
# run is worth keeping in the comment, because it is what the shapes table and the token cap are
# both answers to: at a 512-token cap the six-drug dictation truncated and the pass fell back
# silently, and with no worked example of a DISCONTINUED drug it returned `ibuprofeno` out of
# "tampoco toma ibuprofeno ... lo suspendimos hace un año" — a drug stopped a year ago, listed as
# current. The corpus run then failed `not invented`, a gate the first pass clears.
#
# Both are fixed and re-measured over the seventeen dictations: item recall 94% -> 96%, dose
# errors 3 -> 1, hallucinations 4 -> 3, provenance and `not invented` 100%, every gate clear.
#
# WHAT IT STILL COSTS, stated because a second call that only advertised its gains would be the
# thing this file exists to prevent: two DUPLICATE drug items the first pass does not produce
# (tr-en-05, tr-en-08), each a drug named twice in the transcript and emitted twice, the second
# without a dose. The scorer counts them — see `duplicateDrugs` — and the prompt's worked example
# already demonstrates the exact pattern and is being read past. It is the next thing to fix.
#
# Absent these keys, a transcript is read by one call and this pack behaves exactly as it did
# before they existed.
medicationPrompt       = "prompts/medication.md"
# Per-language, for the reason every other prompt in this file is: the pass copies spans out of a
# transcript, and a model instructed in the wrong language starts translating what it copies.
medicationPromptEs     = "prompts/medication.es.md"
# Its own schema, because the four-section one cannot express "medication only" — every property
# there is required, so a model handed it must fill history and plan too, which is precisely the
# decode competition this pass exists to remove. The ITEM definition inside it is the note-format
# item byte for byte, and a test compares them: an item this pass produces must be
# indistinguishable from one the four-section reading produces, or a shipped note answers to two
# different rules depending on which half of it you read.
medicationSchema       = "schemas/medication.schema.json"
medicationSchemaGolden = "schemas/medication.golden.json"

# --- The repair pass: the failed citations of a reading, handed back with the transcript. ---
# A SECOND contract rather than a second call on the first, because it asks a different
# question. The transcript task asks what this consultation contained; this one asks where in
# the recording a specific item came from, having been told that the answer given is not there.
# Its own schema follows from that — the reply is a list of repairs keyed to items, not a
# reading — and its own prompt follows from the schema.
#
# THIS PASS CERTIFIES NOTHING. Every repair it proposes is re-run through the same
# [clinical.quoteVerification] and [clinical.textDerivation] rules as the first pass, and a
# repair that does not verify is discarded with the original item left standing in its original
# failed state. That is what makes a second turn admissible here at all: the model gets to
# propose a span and never to bless one, so the pass can raise the number of items a clinician
# can trust and cannot raise how much of the reading LOOKS trustworthy.
#
# Absent these keys a reader runs the first pass alone and behaves exactly as it did before
# they existed. The repair is a declared part of the contract or it does not happen.
transcriptRepairPrompt       = "prompts/transcript-repair.md"
# Per-language, for the reason transcriptPromptEs is: the pass reads a transcript and writes
# spans copied out of it, and a model instructed in the wrong language starts translating what
# it copies — the exact failure this pass is supposed to be repairing.
transcriptRepairPromptEs     = "prompts/transcript-repair.es.md"
transcriptRepairSchema       = "schemas/transcript-repair.schema.json"
transcriptRepairSchemaGolden = "schemas/transcript-repair.golden.json"

# --- The literature pass: a finished note in, the concepts worth looking up out. ------
# NOT GRADED BY THIS PACK, and that is stated here rather than left to be discovered. There is
# no `literatureCases` key and no answer key, because "was that the right thing to search for"
# is a judgement this corpus has no way to settle: the reply is not a set of facts recoverable
# from the document, it is an editorial choice about what a clinician would want to read. The
# contract lives here anyway, for the reason every prompt in this file does — a consuming
# application that wrote its own prompt would be shipping instructions nobody could inspect,
# and the eval could not grade this task later without first taking it back off them.
#
# What IS checkable travels with the reply and is checked by the application: every topic
# carries a `source` copied out of the note, verified by literal search under
# [clinical.quoteVerification] exactly as a note item's quote is. A topic whose source is not
# in the note is discarded before anything leaves the machine.
literaturePrompt       = "prompts/literature.md"
literatureSchema       = "schemas/literature.schema.json"
literatureSchemaGolden = "schemas/literature.golden.json"

# The model declaration. Read by the profile for its per-task sampling, and by anyone who
# wants to reproduce a number: a result quoted without the weights and the cap it ran
# under is not reproducible.
models = "models.default.toml"

# --- Settings for the profile that reads this pack. -----------------------------------
#
# Inert to the harness core, which acts only on `spec`, `name`, `files`, `documents` and
# `include`. Everything below is read by src/profiles/clinical/, and lives here rather
# than in that code because it is part of the contract: a rule the eval applies and the
# consuming application does not is a rule that ships unverified.
[clinical]
# These notes were written for this corpus. That is what permits the harness to trace
# completions verbatim, and it is stated here as DATA rather than in the comment at the top of
# this file because the redactor has to read it: a pack that does not set this is treated as
# holding real records and its traces are elided to a digest. Setting it true on a pack of
# real notes is the one line in this file that can put patient data on disk.
corpusSynthetic = true

# `json_schema.name` in the request body. llama.cpp does not act on this label, but two
# runtimes sharing this pack must send the SAME one or their pinned bodies differ for no
# reason.
vitalSignsSchemaName = "vital_signs"
summarySchemaName    = "patient_summary"
noteFormatSchemaName = "note_format"
# Its own label even though it sends the same schema, because the label is what a server log
# and a pinned request body are read by: two tasks that produce different numbers should not
# be indistinguishable in the record of what was sent.
transcriptSchemaName = "dictated_note"
transcriptRepairSchemaName = "quote_repair"
# The medication pass. Its own label like every other task: a server log and a pinned body must
# be able to tell which of the two calls a dictation makes produced which bytes.
medicationSchemaName = "current_medication"
# The literature pass. Its own label like every other task, so a server log and a pinned body
# can tell which contract produced which bytes.
literatureSchemaName = "literature_search"

# Which task `eval --profile clinical` runs when none is named. `--task all` runs the four,
# and each clears its own floor: averaging them would let a ceiling on one hide a failure in
# another, which matters here because vital signs is the mature task and the other two are
# not.
defaultTask = "vital-signs"

# How a note-format `quote` is checked against the note it came from.
#
# The rule is HERE rather than in the code because it is part of the contract: a quote an
# application accepts and the eval rejects is worse than no check at all — the feature would
# ship looking verified. It is deliberately not a regex; see src/core/verify.ts.
[clinical.quoteVerification]
# Collapse every run of whitespace to a single space on BOTH sides before comparing. The
# notes are hard-wrapped at ~78 columns, so a quote spanning a line break carries a newline
# where the model wrote a space; without this every quote longer than one line fails and the
# axis measures the wrapping rather than the model.
collapseWhitespace = true
# Case is compared STRICTLY. "Copy it character by character" is the instruction, and a model
# that lowercases a sentence-initial capital is already editing rather than copying. The eval
# reports the edited-only rate alongside, so a run that fails only on capitalisation stays
# distinguishable from one that fabricated a span.
caseSensitive = true
# Accents likewise: `presión` is not `presion`. Stated because the two tasks that verify
# quotes used to disagree about it in silence — note formatting compared accents strictly and
# vital signs stripped them, so one pack produced two different provenance measurements and
# printed them in the same table. This rule now governs BOTH, and an accent-only failure is
# reported apart from a fabrication exactly as a case-only one is.
#
# It applies to the QUOTE, which is a copy. It does not touch how an ITEM is matched against
# an answer key: "fibrilación" and "fibrilacion" are the same fact, and set matching stays
# accent-insensitive in src/profiles/clinical/set-scorer.ts.
accentSensitive = true

# How an item's `text` — and a medication's `dose` — may differ from its `quote`.
#
# DELETION ONLY: every word in the derived field must already be in the quote, in the same
# order. This is the half of provenance that quote verification alone does not give you, and
# it was paid for by a measured run on a sibling pack: the model emitted a quote reading
# "antibiotic cover with ceftriaxona 2 g every 24 hours", which verifies character for
# character, beside the item "ceptriaxona 2 g every 24 hours". Provenance was perfect and the
# line a clinician reads named a drug that does not exist.
#
# The cost is that the model may not add a connective to make a fragment read on its own.
# That is a real loss and it is worth it: an item that reads slightly rougher is a
# clinician's problem for one second, and a drug name that drifted is a problem for as long
# as the record exists.
[clinical.textDerivation]
deletionOnly = true

# How an ITEM is matched against the answer key, as opposed to how a QUOTE is checked against
# the note. Two different questions: a quote is a copy and is compared strictly, an item is a
# paraphrase and is compared case- and accent-insensitively — "fibrilación" and "fibrilacion"
# are the same fact.
#
# The negator list is the part that has to be data. Plain containment credited the expectation
# `diabet` against the item "No diabetes mellitus" — the opposite fact, counted as a find — so
# a match is refused when one of these words opens the same clause. While the list lived in
# src/profiles/clinical/set-scorer.ts it was code rather than contract, which meant a pack in
# a third language inherited eleven English and Spanish words, matched none of its own
# negations, and had every negated item scored as a find. Silently, and in the direction that
# inflates recall.
#
# These are exactly the words the harness would default to. They are written out anyway,
# because a corpus in two languages should say which two rather than leave a reader to infer
# it from a score. A pack in another language replaces the list; a pack that omits the table
# inherits this one and is told so in spec/pack.md.
# --- Which language a transcript is in, and which prompt that chooses. -----------------
#
# OPTIONAL, and optional for the same reason `setMatching` is rather than the reason
# `quoteVerification` is not. An absent verification rule fakes a passing check; an absent
# table here fakes nothing — every transcript resolves to `transcriptPrompt` and both
# runtimes behave exactly as they did before this table existed. A pack that ships one
# prompt simply omits it.
#
# It is HERE, in the pack, and not in either runtime, because two runtimes that detect
# language separately grade different inputs while appearing to share a contract. The
# consuming app picks a prompt on this rule and the eval measures the prompt this rule
# picks, or the numbers describe something adjacent to the product.
[clinical.languageDetection]
# Function words, not a model. The two languages are separated by their closed class — the
# articles, prepositions and pronouns a clinical dictation cannot avoid — and over a
# paragraph that is not a close call. Deliberately not a detector with an opinion about
# forty languages: it answers one question, it needs no weights, and it runs on a path
# where a download would be absurd.
#
# Chosen for DISJOINTNESS rather than for frequency, which is why the obvious ones are
# missing: `no`, `a`, `me` and `son` are words in both languages, and a marker that scores
# for both sides scores for neither. Compared case- and accent-insensitively, so `está` and
# `esta` are one marker.
[clinical.languageDetection.markers]
es = ["de", "la", "el", "los", "las", "que", "con", "para", "por", "se", "una", "un", "del", "y",
      "en", "es", "su", "más", "está", "tiene", "desde", "pero", "como", "esta", "señora", "señor"]
en = ["the", "and", "of", "to", "is", "for", "with", "on", "at", "was", "she", "he", "her", "his",
      "this", "that", "has", "from", "here", "about", "we", "it", "are", "were", "mrs", "mister"]

[clinical.languageDetection.thresholds]
# How many marker hits, and how large a lead, before the answer is worth giving. Both are
# floors under a corpus that clears them by a distance — the closest of this pack's thirteen
# transcripts wins its language by more than a factor of three. They exist for the input a
# detector will actually be handed on a bad day: a one-line paste, a recording that captured
# a cough and a name. Three hits and a 2x lead is roughly "a clause, decisively".
#
# Below them the answer is NO ANSWER, and no answer means `transcriptPrompt`. A harness that
# guessed a language for a fragment would pick a prompt on a coin flip and pin the result.
minHits = 3
minRatio = 2

# language -> the FILE KEY of the prompt that language gets. A language absent from this map
# is not an error and not a missing translation: it is a language whose transcripts take the
# default prompt, which is what every language did before this table.
[clinical.languageDetection.transcriptPrompt]
es = "transcriptPromptEs"

# The same routing for the dialogue prompt. Shape and language are two independent questions
# asked in that order: the shape picks WHICH FAMILY of prompt, the language picks which member of
# it, and a pack that has a dialogue prompt in one language only falls back to the dialogue
# prompt it has rather than to the dictation one — a wrong-language prompt for the right input
# shape is the smaller of the two errors, and the language rule inside the prompt still holds.
[clinical.languageDetection.dialoguePrompt]
es = "dialoguePromptEs"

# The same routing for the repair pass, kept as its own table rather than shared with the one
# above. The two prompts are separate files and a pack may translate one without the other:
# a pack with a Spanish first pass and no Spanish repair should run the repair in English
# rather than fail to run it, and a single table would make that state inexpressible.
[clinical.languageDetection.transcriptRepairPrompt]
es = "transcriptRepairPromptEs"

# And the same for the medication pass, its own table for the same reason: a pack may translate
# one pass and not the other, and a shared table would make that state inexpressible.
[clinical.languageDetection.medicationPrompt]
es = "medicationPromptEs"

# --- Which transcripts are conversations, and therefore take the dialogue prompt. -----
#
# OPTIONAL on the same terms as [clinical.languageDetection]: absent, every transcript is read as
# a dictation and both runtimes behave exactly as they did before this table existed. It is HERE
# and not in either runtime for the reason the language markers are — two runtimes that decide
# the shape of an input separately are grading different inputs while appearing to share a
# contract, and the consuming application decides it on every recording it handles.
#
# The signal is TURN LABELS, not a model and not a heuristic about question marks. A dictation
# has one voice and no labels; a transcribed consultation has a label on nearly every line,
# because that is what a transcriber writes to say who spoke. Declaring the labels as data is the
# whole point: another pack's transcriber writes "clinician:"/"patient:", "d:"/"p:", or
# "médico:"/"paciente:", and a list in code would silently read all of those as dictations.
[clinical.dialogueDetection]
# Compared case- and accent-insensitively against the text before the colon, so "Dr:" and "dr:"
# are one label. A line is a turn when it begins with one of these followed by a colon.
labels = ["dr", "doctor", "pt", "patient", "med", "medico", "pac", "paciente", "clinician", "d", "p"]
# Two floors, and both are needed. `minTurns` keeps a stray "plan:" or a single quoted line from
# turning a dictation into a consultation; `minSpeakers` keeps a monologue that happens to be
# labelled — a dictation with "dr:" at the top of every paragraph — out, because one voice is
# what the dictation prompt is for however it is punctuated. The corpus clears both by a
# distance: its three dialogues carry 13, 21 and 15 labelled turns across exactly two speakers,
# and no dictation carries one.
minTurns = 4
minSpeakers = 2

# --- Which input shapes take the medication pass. --------------------------------------
#
# THE BOUNDARY IS MEASURED, not cautious. Dictations gain three drugs and three doses over nine
# cases; the same pass over the three dialogues emits a drug the consultation retracted, keeps a
# superseded cross-speaker dose beside the corrected one, and drops derivation to 5/12. Two
# different input shapes, two different answers, and the pack already knows which is which —
# [clinical.dialogueDetection] above decides it, and this table names what that decision buys.
#
# It is DATA because the consuming application makes the same decision on every recording it
# handles. A runtime that ran the pass on everything would ship the tr-en-19 failure to a
# clinician while this eval reported the dictation numbers.
#
# A shape absent from this list takes one call, which is what every shape did before this pass
# existed. Removing the table entirely turns the pass off everywhere.
[clinical.medicationPass]
shapes = ["dictation"]

[clinical.setMatching]
negators = ["no", "not", "never", "denies", "denied", "without", "sin", "niega", "ningun", "ninguna", "nunca"]

# --- What a medication item's `text` may contain: a drug name, and nothing else. --------
#
# The THIRD check on a medication item, and it exists because the other two cannot see this
# failure. `"metformin five hundred milligrams twice daily"` verifies as a quote, derives from
# that quote by deletion alone, and matches the expectation `metformin` by containment: three
# green axes over an item that breaks the plainest rule in prompts/transcript.md. It was found
# on a dictated transcript where a model, pushed to stop merging three drugs into one item,
# split it into three items that each still carried their dose — the merge trap in
# transcript-cases.json went quiet, every number improved, and the reading was still wrong.
#
# It is a rule about SHAPE and not a drug dictionary, and it cannot be otherwise: a check that
# knew which words are drugs would need a list of every drug, which is the thing a pack in a new
# specialty must never have to supply. A drug name is short and contains no dose. That is enough
# to catch every failure of this kind measured so far.
#
# OPTIONAL, on [clinical.setMatching]'s terms rather than [clinical.quoteVerification]'s: an
# absent table fakes no passing check, it inherits a list. These are exactly the words the
# harness would default to, written out for the reason the negators are — a corpus in two
# languages should say which two.
[clinical.medicationName]
# Three: "folic acid" is two words and so is "alendronic acid", while a combination written
# "co-amoxiclav 500/125" is one. A cap rather than a match, because the failure this catches is
# a name with a dose stapled to it, and that is always longer than the name.
maxWords = 3
# Words that belong in `dose` and can never be part of a drug's name. A token containing a DIGIT
# is rejected without being listed here, which is what catches "850" and "2,5" in either
# language. Route words ("oral", "inhalada") are deliberately absent: they are wrong in `text`
# too, but they appear inside real drug names often enough that a check built on them would fail
# good items — and this check is only worth having if a failure it reports is always a failure.
doseTokens = ["mg", "g", "mcg", "ml", "microgram", "micrograms", "milligram", "milligrams",
              "miligramo", "miligramos", "microgramo", "microgramos", "gramo", "gramos",
              "daily", "weekly", "once", "twice", "morning", "night", "nightly", "hourly",
              "hours", "times", "required", "prn",
              "diario", "diaria", "dia", "noche", "manana", "horas", "veces", "cada",
              "semanal", "precisa"]

# The summary task's user message is ASSEMBLED from many notes rather than being one
# document, so the assembly rule is part of the contract. See src/core/assemble.ts.
[clinical.summaryAssembly]
# Per-note cap, applied on a CHARACTER boundary — never a raw byte slice. A cut between the
# two bytes of an "á" is a replacement character here and a panic in a runtime that reads
# this pack from Rust.
perNoteBytes = 2500
# Running total after which assembly stops. Checked AFTER appending, so the note that crosses
# the line is included whole and the marker follows it.
totalChars = 10000
# 1-based index, note type, then content. No space after the period — matched verbatim.
lineFormat = "{i}.{note_type}-{content}\n"
truncationMarker = "...[further notes truncated]"
# Pinned to one value because varying it would measure an input no application produces.
noteType = "Progress note"
